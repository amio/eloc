{
  "name": "@amio/md-editor",
  "version": "0.1.0",
  "description": "A minimal Markdown editor web component with syntax highlighting.",
  "repository": "https://github.com/amio/eloc/tree/main/packages/md-editor",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "module": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --format=iife --outfile=dist/index.js",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "marked": "^17.0.3"
  }
}
